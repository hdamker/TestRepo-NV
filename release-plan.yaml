# CAMARA Release Plan - TEST CASE R9: Version with Extension
# This tests schema pattern validation for API version

repository:
  release_track: meta-release
  meta_release: Fall26
  target_release_tag: r4.1
  target_release_type: pre-release-rc

dependencies:
  commonalities_release: r3.4
  identity_consent_management_release: r3.3

apis:
  - api_name: number-verification
    target_api_version: 1.0.0-rc.1  # INVALID - no extensions allowed
    target_api_status: rc
    # Expected: ERROR - pattern ^\d+\.\d+\.\d+$ does not match
