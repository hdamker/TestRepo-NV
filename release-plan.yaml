# CAMARA Release Plan - TEST CASE R5: public-release + draft mismatch
# This tests semantic validation for release type / API status compatibility

repository:
  release_track: meta-release
  meta_release: Fall26
  target_release_tag: r4.1
  target_release_type: public-release  # Requires all APIs to be 'public'

dependencies:
  commonalities_release: r3.4
  identity_consent_management_release: r3.3

apis:
  - api_name: number-verification
    target_api_version: 1.0.0
    target_api_status: draft  # MISMATCH - not allowed in public-release
